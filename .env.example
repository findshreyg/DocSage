# DocSage Environment Configuration Template
# Copy this file to .env and fill in your actual values
# This file contains all environment variables used across all DocSage services

# ===== AWS Configuration =====
# Required for all services that interact with AWS
AWS_ACCESS_KEY_ID=AKIA_EXAMPLE_KEY_ID_HERE
AWS_SECRET_ACCESS_KEY=example_secret_access_key_replace_with_real_value
AWS_REGION=us-east-1

# ===== AWS S3 Configuration =====
# Used by: file_services, llm_services
S3_BUCKET_NAME=your-docsage-bucket-name

# ===== AWS Cognito Configuration =====
# Used by: auth_services, conversation_services, llm_services
# Note: COGNITO_APP_CLIENT_ID is the exact variable name used in code
COGNITO_USER_POOL_ID=your_cognito_user_pool_id
COGNITO_APP_CLIENT_ID=your_cognito_app_client_id
COGNITO_CLIENT_SECRET=your_cognito_client_secret

# ===== DynamoDB Configuration =====
# Used by: auth_services, file_services, conversation_services, llm_services
# These are typically standard names, but can be customized
DDB_TABLE=IDPMetadata
DYNAMODB_CONVERSATION_TABLE=IDPConversation

# ===== Mistral AI Configuration =====
# Used by: file_services, llm_services
MISTRAL_API_KEY=your_mistral_api_key_here
MISTRAL_API_URL=https://api.mistral.ai/v1/chat/completions
MISTRAL_LLM_MODEL=mistral-large-latest

# # ===== JWT Configuration =====
# # Used by: auth_services (optional for custom JWT handling)
# JWT_SECRET_KEY=your_jwt_secret_key_here
# JWT_ALGORITHM=HS256

# ===== Service URLs (for orchestrator) =====
# Used by: main.py (API Gateway)
AUTH_SERVICE_URL=http://localhost:8000
FILE_SERVICE_URL=http://localhost:8002
CONVERSATION_SERVICE_URL=http://localhost:8001
LLM_SERVICE_URL=http://localhost:8003

# ===== Application Configuration =====
# Used by: all services
LOG_LEVEL=INFO
DEBUG=false

# ===== Security Configuration =====
# Used by: all services
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
MAX_FILE_SIZE_MB=50
ALLOWED_FILE_EXTENSIONS=.pdf,.docx,.doc,.pptx,.ppt,.xlsx,.xls,.txt

# ===== Rate Limiting =====
# Used by: all services with security middleware
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_BURST=10

# ===== Service-Specific Ports (for Docker Compose) =====
# These are used internally by Docker Compose and can be customized
AUTH_SERVICE_PORT=8000
FILE_SERVICE_PORT=8002
CONVERSATION_SERVICE_PORT=8001
LLM_SERVICE_PORT=8003
GATEWAY_PORT=8000

# ===== Database Configuration (if using external databases) =====
# Optional: For production deployments with external databases
# DATABASE_URL=postgresql://user:password@localhost:5432/docsage
# REDIS_URL=redis://localhost:6379/0

# ===== Monitoring & Observability (optional) =====
# Optional: For production monitoring
# SENTRY_DSN=your_sentry_dsn_here
# PROMETHEUS_ENABLED=true
# JAEGER_ENDPOINT=http://localhost:14268/api/traces

# ===== Production Configuration =====
# Optional: For production deployments
# ENVIRONMENT=production
# CORS_ORIGINS=https://yourdomain.com
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem